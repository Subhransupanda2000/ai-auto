/**
 * One-time demo data seeding, run via {@link com.healthcareai.seed.DemoDataSeeder}
 * on application startup. Populates doctors, patients, appointments, and
 * knowledge base articles with realistic (DataFaker-generated) data so the
 * application starts with a fully populated database ready for demo,
 * without ever duplicating data on repeated restarts.
 */
package com.healthcareai.seed;
