/**
 * Custom exceptions and the global exception handler ({@code @RestControllerAdvice})
 * providing consistent error responses and fallback handling for external
 * service failures (LLM timeouts, calendar/notification failures).
 */
package com.healthcareai.exception;
